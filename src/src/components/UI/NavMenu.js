import React from "react";

export default function NavMenu(){
    return(
        <nav>
            <a onClick={77}>Главная</a>
            <a onClick={77}>Наши работы</a>
            <a onClick={77}>Полезная информация</a>
            <a onClick={77}>Доставка</a>
            <a onClick={77}>Заказ</a> {/*Предварительный расчет, описание о заказе, статус заказа*/}
            <a onClick={77}>Кто мы?</a>
        </nav>
    )
}